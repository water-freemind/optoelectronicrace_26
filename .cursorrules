# 仓库级全局规则（供本地 OpenCode/助手参考）

规则: 始终用中文回复
- 默认行为: 在与用户交互时，助手应始终使用中文（简体）进行回复。
- 例外1: 如果用户明确要求使用其他语言，则遵从用户要求。
- 例外2: 如果系统或开发者级别指令与此规则冲突，应遵循系统/开发者指令优先级（本文件为仓库级建议）。

目的: 提供一个简短、可读的仓库级说明，帮助未来的 OpenCode 会话快速遵守团队偏好——中文输出为默认语言。

位置: 仓库根目录下的 .cursorrules 文件。此文件为说明性配置；具体集成取决于本地 OpenCode/代理实现。
